import "package:flutter/material.dart";
class FollowButton extends StatelessWidget {
  Color color;
  String stringText;
  var funtion;
   FollowButton({super.key, required this.color, required this.stringText, required this.funtion});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}